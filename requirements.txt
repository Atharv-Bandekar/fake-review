create virtual env in your project's directory:
python -m venv venv

activate it:
venv/bin/activate

install the following in virtual enivironment:
pip install pandas numpy scikit-learn jupyter tensorflow 
